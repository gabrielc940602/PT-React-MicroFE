export function CurrencyExchangeHeader() {
    const className = "ppscurrencyexchangeheader";
    return (
        <header className={className}>
            <h1 className={`${className}__title`}>Currency exchange</h1>
        </header>
    );
}
